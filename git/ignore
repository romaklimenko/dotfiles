# Global git ignore, installed from dotfiles (git/ignore) to
# ~/.config/git/ignore. Git reads that path when core.excludesFile is unset.
# Tracked files are unaffected: `git add -f LESSONS.md` opts a project in.

# Claude Code per-machine settings
**/.claude/settings.local.json

# Session notes written by the Claude Code lessons hook
LESSONS.md
